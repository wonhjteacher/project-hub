# PRD — PROJECT HUB

| 항목   | 내용                                   |
| ------ | -------------------------------------- |
| 문서명 | PROJECT HUB 제품 요구사항 정의서 (PRD) |
| 버전   | v1.5                                   |
| 작성일 | 2026-08-13                             |
| 작성자 | 원혜종                                 |
| 상태   | 개발 진행 중 (인트로·메인 구현 완료 / 프로젝트 01·02·03 배포 완료) |

**변경 이력**

| 버전 | 일자       | 변경 내용                                                                     |
| ---- | ---------- | ----------------------------------------------------------------------------- |
| v1.0 | 2026-08-13 | 초안 확정                                                                     |
| v1.1 | 2026-08-13 | 카드 배치를 **그리드 → 무한 슬라이더**로 변경 (CRD-02 개정, CRD-09~11 신설) |
| v1.2 | 2026-08-14 | R2 대응을 **등장(1.2초, 스킵 필수) / 전환(0.6초)** 으로 분리. 미해결 이슈 **O1 확정** — 서비스명은 `PROJECT HUB` |
| v1.3 | 2026-08-14 | About을 별도 페이지(`about.html`)로 분리, IA 개정 (4장 참조) |
| v1.4 | 2026-08-14 | **프로젝트 01 배포 완료** — 5.2 목록표 01행 확정, `Coming Soon` 단일 상태 전제 해제. CRD-04 개정, CRD-12 신설, MOD-12 신설, 6장 데이터 모델에 `status`·`liveUrl`·`thumbnail` 추가 |
| v1.5 | 2026-08-18 | **프로젝트 02·03 배포 완료** — 5.2 목록표 02·03행 확정. v1.4에서 만든 `status`·`liveUrl`·`thumbnail` 규격을 그대로 적용했고, 신설·개정된 요구사항은 없다 |

---

## 1. 서비스 개요

### 1.1 한 줄 정의

**PROJECT HUB**는 내가 기획하고 만들어 나갈 프로젝트들을 한곳에 모아, 방문자가 3분 안에 "이 사람은 어떤 문제를 어떻게 푸는 사람인가"를 판단할 수 있게 하는 개인 프로젝트 아카이브다.

### 1.2 배경

- 여러 프로젝트가 개별 문서·파일로 흩어져 있어, 외부에 보여줄 단일 진입점이 없다.
- 완성작 중심의 일반 포트폴리오와 달리, **현재는 착수·기획 단계의 프로젝트가 대부분**이다.
- 따라서 이 서비스는 "결과물 전시장"이 아니라 **"문제의식과 기획력의 전시장"**으로 설계한다.

### 1.3 핵심 설계 원칙

| #   | 원칙 | 이유 |
| --- | ---- | ---- |

| P1 | **결과물이 없어도 사고 과정은 보여줄 수 있다** | 8개 전부 미완성이므로, 상세 모달이 실질적 핵심 콘텐츠다. |
| P2 | **모바일 우선 대응** | 링크로 공유받아 휴대폰에서 먼저 열어보는 경우가 많다. |
| P3 | **깊이는 최대 2단계** | 인트로 → 리스트 → 모달. 그 이상 들어가지 않는다. |

---

## 2. 타겟 사용자

### 2.1 주 타겟

**채용 담당자 / 클라이언트** (포트폴리오 성격)

| 구분        | 내용                                                 |
| ----------- | ---------------------------------------------------- |
| 상황        | 지원서·제안서에 첨부된 링크를 받아 처음 방문         |
| 목적        | 짧은 시간 안에 역량과 관심 분야를 파악               |
| 체류 시간   | 1~3분 (첫 판단은 20초 이내)                          |
| 주 디바이스 | 모바일 → 관심이 생기면 데스크톱 재방문               |
| 사전 지식   | 도메인 지식은 있으나, 개별 프로젝트 맥락은 전혀 모름 |

### 2.2 타겟이 던지는 3가지 질문

이 서비스는 아래 3개 질문에 답하는 것을 목표로 한다.

1. 이 사람은 **무엇에 관심**이 있는가?
2. 이 사람은 문제를 **어떻게 정의하고 접근**하는가?
3. 연락하려면 **어떻게** 해야 하는가?

---

## 3. 핵심 리스크와 대응

> **본 PRD에서 가장 중요한 항목이다.**

### R1. 8개 카드가 전부 `Coming Soon`이다

| 항목   | 내용                                                                                                                                                     |
| ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 리스크 | 방문자에게 "아직 아무것도 완성하지 못한 사람"으로 읽힐 수 있다. 카드가 많을수록 미완성의 인상이 증폭된다.                                                |
| 대응 1 | 상세 모달에 **문제 정의 → 타겟 → 핵심 기능 → 기술 스택**의 4단 구조를 채워, *결과물 대신 기획의 밀도*로 승부한다. 모달 내용이 비면 이 서비스는 실패한다. |
| 대응 2 | About 섹션에서 이 허브가 **"진행형 아카이브"**임을 명시적으로 선언한다. 미완성을 감추지 않고 프레이밍한다.                                               |
| 대응 3 | `Coming Soon`을 별도 배지가 아니라 **썸네일 영역 내부**에 처리해, 시각적으로 반복 강조되지 않게 한다.                                                    |
| 대응 4 _(v1.4)_ | 배포된 프로젝트는 **실제 첫 화면 캡처**를 썸네일에 넣는다. 텍스처만 깔린 카드 사이에서 실물 화면 한 장이 리스크를 가장 직접적으로 줄인다. 이 리스크는 8개가 채워질수록 자연 소멸한다. |

> **v1.4 진행 상황 —** 8개 중 1개(`오늘은 여기`)가 배포되어 R1의 전제가 부분적으로 해소되었다. 남은 7개가 채워지는 만큼 이 리스크는 줄어든다.

### R2. 인트로 페이지가 이탈 지점이 될 수 있다

| 항목   | 내용                                                                                                                                                  |
| ------ | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| 리스크 | ENTER를 눌러야 콘텐츠가 나오는 구조는 한 단계의 마찰을 추가한다.                                                                                      |
| 대응   | **전환** 애니메이션은 **0.6초 이내**를 유지한다. **등장** 연출(SplitText 산포)은 **1.2초**까지 허용하되, **어떤 입력이든 즉시 건너뛸 수 있어야 한다**(v1.2 개정 — 아래 참고). ENTER 버튼은 첫 화면에서 즉시 보이고, 터치 영역을 충분히 확보한다. 스크롤·키보드(Enter)로도 진입 가능하게 한다. |
| 참고   | v1.2에서 "총 1초 이내"를 등장/전환으로 분리했다. 마찰의 실체는 *기다려야만 하는* 시간이지 연출의 길이 자체가 아니므로, 스킵이 보장되는 등장 연출은 상한을 완화하고 스킵할 수 없는 전환은 0.6초로 조인다. |

### R3. 8개 카드 중 상세 내용이 부실한 카드가 섞인다

| 항목   | 내용                                                                                                                          |
| ------ | ----------------------------------------------------------------------------------------------------------------------------- |
| 리스크 | 한 카드라도 내용이 비면 전체 신뢰도가 떨어진다.                                                                               |
| 대응   | **4개 필드(문제 정의/타겟/핵심 기능/기술 스택)를 모두 채우지 못한 프로젝트는 카드로 올리지 않는다.** 8개 미만이어도 무방하다. |

### R4. 움직이는 카드가 클릭을 방해할 수 있다 _(v1.1 신설)_

| 항목   | 내용                                                                                                                                                                       |
| ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 리스크 | 자동 이동하는 카드는 클릭 타겟이 움직인다는 뜻이다. 성공 지표인 **모달 열람률(50%)·평균 열람 카드 수(2개)**를 직접 깎을 수 있다. 모바일에서는 세로 스크롤과 제스처가 충돌한다. |
| 대응 1 | **호버·포커스 시 즉시 정지**한다. (CRD-09) 커서를 올린 순간 타겟이 멈추므로 실제 클릭 시점에는 정지 상태다.                                                                |
| 대응 2 | **드래그로 직접 조작**할 수 있게 해, 기다리지 않고 원하는 카드에 도달하게 한다. (CRD-10)                                                                                   |
| 대응 3 | 터치 영역에 `touch-action: pan-y`를 적용해 **세로 스크롤은 브라우저에 넘기고 가로 제스처만 가져온다.** (CRD-11)                                                            |
| 대응 4 | 이동 속도를 **52px/sec**로 낮게 잡는다. 읽는 속도를 앞지르지 않는 것이 기준이다.                                                                                           |
| 측정   | 배포 후 모달 열람률이 목표(50%)에 미달하면 **자동 이동을 끄고 드래그 전용으로 전환**한다. 되돌리는 비용이 낮도록 구현되어 있다.                                             |

---

## 4. 정보 구조 (IA) 및 화면 흐름

```
[화면 1] 인트로 (index.html)
   PROJECT HUB
   [ ENTER ]
        │
        ▼
[화면 2] 프로젝트 리스트 (project-list.html)
   └─ 프로젝트 카드 슬라이더 (8개, 무한 루프)
        │             │
        │             ▼ 카드 클릭
        │     [화면 4] 프로젝트 상세 모달
        │        - 문제 정의
        │        - 타겟 사용자
        │        - 핵심 예정 기능
        │        - 사용 기술 스택
        │
        ▼ 상단 내비게이션
[화면 3] About (about.html)
   ├─ ① About 섹션      (상단)
   └─ ② Contact 섹션    (하단 / 푸터)
```

- 총 페이지 수: **3개** (인트로 / 리스트 / About)
- 오버레이: **1개** (상세 모달)
- 페이지 간 이동은 **상단 내비게이션** 하나로만 처리한다. 그 외 라우팅은 늘리지 않는다.

> **v1.3 개정 — About을 별도 페이지로 분리:**
> 원안은 About·리스트·Contact를 "메인" 한 페이지에 담고 별도 라우팅 페이지를 금지(P4)했다.
> 그러나 구현 단계에서 리스트가 `project-list.html`로 분리되면서, About을 리스트 최상단에 두면
> **카드 캐러셀에 닿기까지 스크롤이 한 번 더 필요**해져 ABT-04(첫 스크롤 없이 보임)와
> 리스트의 즉시 열람성이 서로를 깎는 상태가 됐다. 둘 다 각자의 화면에서 첫 화면을 차지하도록 분리한다.
> 깊이는 여전히 최대 2단계(P3)다 — About은 리스트와 **같은 층**의 형제 페이지이지, 하위 단계가 아니다.
> 이에 따라 P4(별도 라우팅 페이지 금지)는 폐기하고, 위의 "내비게이션 하나로만 처리한다" 규칙으로 대체한다.

---

## 5. 화면별 상세 요구사항

### 5.1 화면 1 — 인트로

| 항목      | 정의                                                          |
| --------- | ------------------------------------------------------------- |
| 목적      | 서비스의 정체성을 각인시키고 즉시 통과시킨다                  |
| 구성 요소 | 서비스명 `PROJECT HUB` + `ENTER` 버튼 **(2개뿐)**             |
| 보조 요소 | `vibe coding` 라벨(로고 위, `--fs-label`) + `press enter` 힌트(하단, `--fs-label`) _(v1.2 신설)_ |
| 제외 요소 | 태그라인, 이름/역할, 배경 이미지, 소개 문구 — **넣지 않는다** |

> **보조 요소는 태그라인이 아니다 (v1.2):** 둘 다 `--fs-label`(12px)로, 읽어야 할 문장이 아니라 **위치를 알려주는 표지**다. `vibe coding`은 허브의 성격을, `press enter`는 즉시 통과 가능함을 알린다. 후자는 R2의 스킵 보장(INT-09)을 사용자에게 노출하는 장치이므로 마찰을 줄이는 쪽으로 작동한다. 문장을 덧붙이는 순간 제외 요소가 되므로 이 두 개에서 늘리지 않는다.

**요구사항**

| ID     | 요구사항                                                                        | 우선순위 |
| ------ | ------------------------------------------------------------------------------- | -------- |
| INT-01 | 화면 중앙에 `PROJECT HUB` 텍스트를 배치한다                                     | Must     |
| INT-02 | 그 아래 `ENTER` 버튼을 배치한다                                                 | Must     |
| INT-03 | ENTER 클릭 시 메인 화면으로 전환한다                                            | Must     |
| INT-04 | 진입 전환 애니메이션은 **0.4~0.8초** 이내로 제한한다                            | Must     |
| INT-05 | 키보드 `Enter` 키로도 진입할 수 있다                                            | Should   |
| INT-06 | 모바일에서 ENTER 버튼 터치 영역은 **최소 44×44px**                              | Must     |
| INT-07 | 한 번 진입한 사용자는 재방문 시 인트로를 건너뛴다 (localStorage 또는 세션 기준) | Could    |
| INT-08 | 로고는 글자 단위로 흩어졌다 수렴하는 **SplitText 산포**로 등장한다 (1.2초) _(v1.2 신설)_ | Should   |
| INT-09 | **연출 중 클릭·키 입력 시 즉시 완료 상태로 건너뛴다.** INT-08의 전제 조건이며, 이것이 없으면 INT-08을 채택하지 않는다 _(v1.2 신설)_ | Must     |

---

### 5.2 화면 2 — 메인

> **v1.3:** 아래 ①·③은 `about.html`(화면 3), ②는 `project-list.html`(화면 2)로 분리됐다.
> 요구사항 ID(ABT-\*, CRD-\*, CNT-\*)와 그 내용은 그대로 유효하며, 배치 페이지만 달라진다.

#### ① About 섹션 (about.html 상단)

| 항목      | 정의                                                                |
| --------- | ------------------------------------------------------------------- |
| 목적      | "누구인가"를 먼저 밝혀, 이어지는 미완성 카드들에 맥락을 부여한다    |
| 포함 내용 | 이름 / 역할 / 2~3문장 소개 / **이 허브가 진행형 아카이브라는 선언** |

| ID     | 요구사항                                                      | 우선순위 |
| ------ | ------------------------------------------------------------- | -------- |
| ABT-01 | 이름과 역할을 명시한다                                        | Must     |
| ABT-02 | 2~3문장 이내의 소개문을 노출한다 (길어지면 읽히지 않음)       | Must     |
| ABT-03 | 프로젝트들이 진행 중임을 밝히는 문장을 포함한다               | Must     |
| ABT-04 | About 페이지 진입 직후 **첫 스크롤 없이** 보이는 위치에 있어야 한다 _(v1.3 — 원문 "인트로 통과 직후")_ | Should   |
| ABT-05 | 리스트 페이지에서 **한 번의 클릭**으로 About에 닿을 수 있어야 한다 (상단 내비게이션) _(v1.3 신설)_ | Must     |

#### ② 프로젝트 카드 그리드

**카드 노출 항목 (4개 — 확정)**

| 항목       | 설명                     | 비고                                     |
| ---------- | ------------------------ | ---------------------------------------- |
| 번호       | `01` ~ `08`              | 순서 = 보여주고 싶은 우선순위            |
| 썸네일     | 이미지 또는 색상 블록    | `Coming Soon` 문구를 이 영역 안에 얹는다 |
| 제목       | 프로젝트명               | 1줄 고정                                 |
| 한 줄 요약 | 무엇을 하는 프로젝트인가 | 최대 40자 권장, 2줄 초과 시 말줄임       |

> 별도의 상태 배지는 **두지 않는다.** 8개 전부 동일 상태이므로 배지가 정보를 전달하지 못하고 시각적 노이즈만 만든다. (R1 대응 3)
>
> **v1.4 개정 —** 프로젝트 01이 배포되면서 이 전제(8개 동일 상태)가 깨졌다. 상태가 갈리는 순간부터 표기는 노이즈가 아니라 정보다. 다만 여전히 **별도 배지 영역을 만들지는 않고**, 썸네일 안의 `Coming Soon` 문구 자리를 `Live` 로 대체하는 방식을 유지한다. 카드 구조는 그대로다.

| ID     | 요구사항                                                         | 우선순위 |
| ------ | ---------------------------------------------------------------- | -------- |
| CRD-01 | 카드는 총 **8개**를 가로 슬라이더로 배치한다                     | Must     |
| CRD-02 | 카드는 **무한 루프**로 순환하며 자동 이동한다 (v1.1 개정)        | Must     |
| CRD-09 | 호버·포커스 시 이동을 멈춘다 (v1.1 신설)                        | Must     |
| CRD-10 | 드래그·스와이프로 직접 이동할 수 있다 (v1.1 신설)               | Should   |
| CRD-11 | 모바일에서 세로 페이지 스크롤을 방해하지 않는다 (v1.1 신설)     | Must     |
| CRD-03 | 각 카드에 번호·썸네일·제목·한 줄 요약을 노출한다                 | Must     |
| CRD-04 | 썸네일 영역에 상태를 표기한다 — 미배포는 `Coming Soon`, 배포 완료는 `Live` (v1.4 개정) | Must     |
| CRD-12 | 배포된 프로젝트의 썸네일에는 **실제 서비스 첫 화면 캡처**를 넣는다 (16:9). 미배포는 CSS 텍스처를 유지한다 (v1.4 신설) | Should   |
| CRD-05 | 카드 전체 영역이 클릭 가능해야 한다 (제목만 X)                   | Must     |
| CRD-06 | 호버 시 클릭 가능함을 나타내는 시각 피드백을 준다                | Should   |
| CRD-07 | 제목·요약 길이가 달라도 카드 높이는 일정하게 유지한다            | Should   |
| CRD-08 | 카드는 데이터 배열로부터 렌더링한다 (하드코딩 금지)              | Must     |

**프로젝트 목록 (8개 — 내용 미확정, 별도 채움 필요)**

| #   | 프로젝트명 | 한 줄 요약 | 상태        |
| --- | ---------- | ---------- | ----------- |
| 01  | 오늘은 여기 | 담아둔 맛집 중에서 오늘 갈 한 곳을 골라드려요. | **Live** — https://review-two-murex.vercel.app/ |
| 02  | 세계 랜드마크 날씨 | 전 세계 명소의 지금 날씨를 한 화면에서 비교해요. | **Live** — https://weather-ten-topaz-48.vercel.app/ |
| 03  | NewsFinder | 흩어진 뉴스를 키워드 하나로 모아서 봐요. | **Live** — https://news-liard-seven.vercel.app/ |
| 04  | _(미정)_   | _(미정)_   | Coming Soon |
| 05  | _(미정)_   | _(미정)_   | Coming Soon |
| 06  | _(미정)_   | _(미정)_   | Coming Soon |
| 07  | _(미정)_   | _(미정)_   | Coming Soon |
| 08  | _(미정)_   | _(미정)_   | Coming Soon |

> ⚠️ **개발 착수 전 반드시 채워야 하는 표다.** 8개의 실제 내용이 이 서비스의 전부이며, 이 표가 비어 있으면 UI만 완성되고 제품은 완성되지 않는다.

#### ③ Contact 섹션 (about.html 하단)

| ID     | 요구사항                                                                                 | 우선순위 |
| ------ | ---------------------------------------------------------------------------------------- | -------- |
| CTC-01 | 이메일 주소를 노출하고, 클릭 시 메일 작성으로 연결한다 (`mailto:wonhjteacher@gmail.com`) | Must     |
| CTC-02 | GitHub / LinkedIn 등 외부 링크를 제공한다                                                | Should   |
| CTC-03 | 외부 링크는 새 탭에서 연다                                                               | Must     |
| CTC-04 | 페이지 최하단(푸터)에 배치한다                                                           | Must     |

---

### 5.3 화면 3 — 프로젝트 상세 모달

| 항목 | 정의                                                     |
| ---- | -------------------------------------------------------- |
| 목적 | 결과물이 없는 상태에서 **기획력을 증명하는 유일한 지면** |
| 방식 | 모달(오버레이). 페이지 이동 없음                         |

**모달 콘텐츠 구조 (4개 — 확정)**

| 순서 | 섹션               | 내용                                     | 분량 가이드 |
| ---- | ------------------ | ---------------------------------------- | ----------- |
| 1    | **문제 정의**      | 왜 이걸 만드는가. 어떤 문제를 관찰했는가 | 2~4문장     |
| 2    | **타겟 사용자**    | 누구를 위한 것인가                       | 1~2문장     |
| 3    | **핵심 예정 기능** | 무엇을 만들 것인가                       | 3~5개 항목  |
| 4    | **사용 기술 스택** | 무엇으로 만들 것인가                     | 태그 나열   |

> 순서를 지킨다. **문제 → 사람 → 해결 → 도구**의 흐름이 곧 기획 역량의 서사다. 기술 스택을 위로 올리면 "도구 나열"로 읽힌다.

| ID     | 요구사항                                                               | 우선순위 |
| ------ | ---------------------------------------------------------------------- | -------- |
| MOD-01 | 카드 클릭 시 모달을 연다                                               | Must     |
| MOD-02 | 상단에 프로젝트 번호·제목·한 줄 요약을 표시한다                        | Must     |
| MOD-03 | 4개 섹션(문제 정의/타겟/핵심 기능/기술 스택)을 순서대로 표시한다       | Must     |
| MOD-04 | 닫기 버튼(×)을 제공한다                                                | Must     |
| MOD-05 | 배경(오버레이) 클릭 시 닫힌다                                          | Must     |
| MOD-06 | `ESC` 키로 닫힌다                                                      | Should   |
| MOD-07 | 모달이 열린 동안 배경 스크롤을 잠근다                                  | Must     |
| MOD-08 | 내용이 길면 모달 내부에서 스크롤된다                                   | Must     |
| MOD-09 | 모바일에서는 전체화면 또는 바텀시트 형태로 전환한다                    | Should   |
| MOD-10 | 모달을 닫으면 직전에 보던 카드 위치로 복귀한다                         | Should   |
| MOD-11 | 열릴 때 포커스를 모달로 이동하고, 닫으면 원래 카드로 되돌린다 (접근성) | Could    |
| MOD-12 | 배포된 프로젝트는 4개 섹션 **위**에 `사이트 방문` CTA를 노출하고, CTC-03에 따라 새 탭에서 연다 (v1.4 신설) | Must     |

> **v1.4 — 카드 클릭은 여전히 모달을 연다 (MOD-01 유지).** 배포된 프로젝트라고 해서 카드에서 곧장 외부 사이트로 보내지 않는다. 이 모달은 기획력을 증명하는 유일한 지면이고(5.3 목적), 결과물이 있는 프로젝트일수록 "문제 → 사람 → 해결 → 도구"의 서사를 먼저 보여준 뒤 실물로 넘기는 편이 강하다. 또한 카드마다 클릭 동작이 갈리면 예측 가능성이 떨어진다. 그래서 진입점은 하나로 두고, 실물로 나가는 문은 모달 안에 만든다.

---

## 6. 데이터 모델

프로젝트 데이터는 단일 배열로 관리한다.

```js
const PROJECTS = [
  {
    num: "01",
    title: "프로젝트명",
    summary: "한 줄 요약 (40자 이내 권장)",

    // 아래 3개는 배포된 프로젝트에만 있다 (v1.4 신설). 없으면 Coming Soon 으로 그려진다.
    status: "live",                        // 생략 시 미배포
    liveUrl: "https://...",                // 모달의 '사이트 방문' CTA 대상 (MOD-12)
    thumbnail: "assets/project-01.jpg",    // 실제 첫 화면 캡처, 16:9 (CRD-12)

    detail: {
      problem: "문제 정의 (2~4문장)",
      target: "타겟 사용자 (1~2문장)",
      features: ["핵심 기능 1", "핵심 기능 2", "핵심 기능 3"],
      stack: ["HTML", "JavaScript", "..."],
    },
  },
  // ... 총 8개
];
```

**규칙**

- `detail`의 4개 필드는 **모두 필수**다. 하나라도 비면 카드를 노출하지 않는다. (R3 대응)
  - 단, 미확정 프로젝트는 `detail` 자체를 생략한다. 이때 각 섹션은 `내용 준비 중입니다.` 자리표시자로 그려진다.
- `num`은 배열 순서와 일치시킨다.
- `status` / `liveUrl` / `thumbnail` 은 **함께 움직인다.** 배포되면 셋을 같이 채우고, 그 전에는 셋 다 없다.
- 프로젝트 추가·수정은 이 배열만 고치면 되도록 구현한다. (CRD-08)

---

## 7. 비기능 요구사항

| 구분     | 요구사항                                                                  |
| -------- | ------------------------------------------------------------------------- |
| 반응형   | 모바일(~767) / 태블릿(768~1023) / 데스크톱(1024~) 3개 브레이크포인트 대응 |
| 성능     | 최초 로딩 2초 이내, 썸네일 이미지 최적화 필수                             |
| 브라우저 | Chrome, Safari, Edge 최신 2개 버전                                        |
| 접근성   | 키보드 탐색 가능(Tab/Enter/ESC), 이미지 `alt` 제공, 명도 대비 확보        |
| SEO      | 페이지 title, description, OG 태그(공유 시 썸네일) 설정                   |
| 유지보수 | 프로젝트 데이터와 화면 로직 분리                                          |

---

## 8. 기술 스택 (제안)

| 옵션         | 구성                         | 적합 상황                                                      |
| ------------ | ---------------------------- | -------------------------------------------------------------- |
| **A (권장)** | 단일 HTML + CSS + Vanilla JS | 페이지 2개·모달 1개 규모에 최적. 배포·수정이 가장 빠르다       |
| B            | React + Vite                 | 추후 프로젝트 수가 크게 늘거나 필터·검색을 붙일 계획이 있을 때 |

- 배포: GitHub Pages / Vercel / Netlify 중 택 1
- 백엔드·데이터베이스 **없음**

---

## 9. 범위 (Scope)

### 9.1 MVP 포함

- 인트로 페이지 + ENTER 진입
- About 섹션
- 프로젝트 카드 슬라이더 (8개, 무한 루프)
- 프로젝트 상세 모달 (4개 섹션)
- Contact 섹션
- 반응형 대응

### 9.2 범위 밖 (Out of Scope)

- 검색 / 필터 / 정렬
- 다크모드 토글
- 다국어 지원
- 방문자 통계·애널리틱스
- 댓글, 좋아요 등 상호작용
- 관리자 페이지 (데이터는 코드에서 직접 수정)
- 블로그 / 아티클

---

## 10. 성공 지표

| 지표              | 목표                                         | 측정 방법                |
| ----------------- | -------------------------------------------- | ------------------------ |
| 인트로 통과율     | 90% 이상                                     | ENTER 클릭 / 방문 수     |
| 모달 열람률       | 방문자의 50% 이상이 최소 1개 열람            | 클릭 이벤트              |
| 평균 열람 카드 수 | 2개 이상                                     | 클릭 이벤트              |
| Contact 도달률    | 20% 이상                                     | 최하단 스크롤 도달       |
| 정성 평가         | "미완성"이 아니라 "준비된 사람"으로 읽히는가 | 지인 3~5명 사용성 테스트 |

---

## 11. 개발 순서 (권장)

| 단계 | 작업                                                  | 선행 조건 |
| ---- | ----------------------------------------------------- | --------- |
| 0    | **8개 프로젝트의 내용 확정** (5.2 ②표 + detail 4필드) | —         |
| 1    | 데이터 배열 작성                                      | 0 완료    |
| 2    | 메인 화면 (카드 슬라이더) 구현                        | 1         |
| 3    | 상세 모달 구현                                        | 2         |
| 4    | 인트로 페이지 구현                                    | 2         |
| 5    | About / Contact 섹션                                  | 2         |
| 6    | 반응형 + 접근성 점검                                  | 3,4,5     |
| 7    | 배포 및 사용성 테스트                                 | 6         |

> **0단계 없이 2단계로 넘어가지 않는다.** 이 프로젝트의 병목은 UI가 아니라 콘텐츠다.

---

## 부록 A. 미확정 이슈 (Open Issues)

| #   | 이슈                                                                  | 결정 필요 시점 |
| --- | --------------------------------------------------------------------- | -------------- |
| ~~O1~~ | ~~서비스명 표기 확정 — `PROJECT HUB` vs `PROJUCT HUB`(의도적 변형 여부)~~ | ✅ **확정 (v1.2) — `PROJECT HUB`**. `PROJUCT` 변형은 채택하지 않는다. 대신 로고 위에 `vibe coding`을 `--fs-label` 라벨로 얹어 성격을 밝힌다 (DESIGN 6.1) |
| O2  | 8개 프로젝트의 실제 목록과 상세 내용                                  | 0단계          |
| O3  | 썸네일 방식 — 실제 이미지 vs 색상 블록 vs 타이포 조합                 | 2단계 전       |
| O4  | About에 들어갈 이름·역할·소개문 실제 텍스트                           | 5단계 전       |
| O5  | Contact에 노출할 채널 (이메일 필수, 그 외 선택)                       | 5단계 전       |
| O6  | 비주얼 톤 — 미니멀 / 레트로 터미널 / 아카이브 등 방향성               | 2단계 전       |
| O7  | 도메인 사용 여부 및 배포 플랫폼                                       | 7단계 전       |

## 부록 B. 결정 근거 추적 (Evidence Chain)

| 결정                                          | 근거                                                                    |
| --------------------------------------------- | ----------------------------------------------------------------------- |
| 인트로를 미니멀(서비스명+ENTER)로 제한        | 타겟(채용 담당자)의 짧은 체류 시간. 진입 마찰 최소화 (P1, R2)           |
| 상태 배지 제거, 썸네일에 통합                 | 8개 전부 동일 상태 → 배지가 정보를 전달하지 못함 (R1)                   |
| 상세를 모달로 처리                            | 인트로→리스트로 이미 1단계 진입. 추가 페이지 이동은 깊이 과다 (P3)      |
| About을 별도 페이지로 분리 _(v1.3)_           | 리스트 분리 후 About을 최상단에 두면 ABT-04와 카드 즉시 열람성이 충돌. 형제 페이지라 깊이는 그대로 (4장 개정 참조) |
| 상세 섹션 순서를 문제→타겟→기능→스택으로 고정 | 기획 서사 구조. 스택 우선 노출 시 "도구 나열"로 읽힘                    |
| Contact를 상단이 아닌 하단 배치               | 방문자 동선: 정체 파악 → 역량 확인 → 연락. 판단 전 연락처 노출은 무의미 |
| About을 상단 배치                             | 8개 Coming Soon 카드에 앞서 맥락을 제공, 미완성 인상 완충 (R1)          |
| 4개 필드 미완성 시 카드 미노출                | 부실한 1개가 전체 신뢰도를 떨어뜨림 (R3)                                |
| 그리드 → 무한 슬라이더 (v1.1)                 | 8개 전부 Coming Soon인 정지 그리드는 "정적인 시안"으로 읽힘. 움직임이 진행형 아카이브라는 메시지와 맞음. 대가로 R4를 감수하고 대응 4건을 붙임 |

---

_문서 끝_
