# DESIGN — PROJECT HUB

| 항목 | 내용 |
|---|---|
| 문서명 | PROJECT HUB 디자인 시스템 정의서 |
| 버전 | v1.2 |
| 작성일 | 2026-08-13 |
| 작성자 | 원혜종 |
| 연관 문서 | `PRD_PROJECT_HUB.md` |
| 상태 | 구현 반영 (D-O1·D-O2·D-O3·D-O4·D-O5 확정) |

**변경 이력**

| 버전 | 일자 | 변경 내용 |
|---|---|---|
| v1.0 | 2026-08-13 | 초안 확정 |
| v1.1 | 2026-08-13 | 카드 배치를 **그리드 → 무한 슬라이더**로 변경(§5.3, §7.3). D-O1~D-O5 확정 |
| v1.2 | 2026-08-14 | 인트로 로고 등장에 **SplitText 산포** 도입(§2.2, §7.2 신설, §7.3~7.6 번호 이동). 캐러셀 랩퍼를 **전체 폭 → 1280px** 고정(§5.2, §5.3). 모달 열기를 **페이드+스케일 → 카드 확대**로 변경(§7.5) |

---

## 1. 디자인 방향 요약

| 항목 | 확정 내용 |
|---|---|
| 무드 | **다크 모던** — 세련되고 밀도 있는, 포인트 컬러 1개 |
| 배경 톤 | **차콜 그레이** (`#121212` ~ `#1A1A1A`) |
| 포인트 컬러 | **일렉트릭 블루** `#3B82F6` |
| 서체 계열 | **모던 산세리프** — Inter(영문) / Pretendard(한글) |
| 로고 타입 | **대형 · 굵게 · 자간 좁게** |
| 언어 운용 | **혼용** — UI 라벨은 영문, 콘텐츠는 한글 |
| 모션 | **적극적** (단, 진입 경로는 절제 — 2.2 참조) |
| 인트로 등장 | **SplitText 산포** (1.2초, 스킵 가능) _(v1.2)_ |
| 인트로 전환 | **줌 인** (0.6초 상한, 스킵 가능) |

---

## 2. 디자인 원칙

### 2.1 핵심 원칙

| # | 원칙 | 설명 |
|---|---|---|
| D1 | **여백이 곧 콘텐츠다** | 인트로에 요소가 2개뿐이고 8개 카드가 전부 Coming Soon이다. 채울 것이 적으므로 여백의 리듬이 완성도를 결정한다. |
| D2 | **어두운 배경에서는 대비가 생명이다** | 다크 모드에서 가장 흔한 실패는 "멋있지만 안 읽힘"이다. 모든 텍스트는 명도 대비 기준(4.5:1)을 통과해야 한다. |
| D3 | **색은 하나만 쓴다** | 블루 외의 유채색을 추가하지 않는다. 색이 늘어나는 순간 다크 모던은 무너진다. |
| D4 | **비어 있음을 디자인한다** | Coming Soon은 감출 대상이 아니라 다뤄야 할 상태다. 빈 썸네일도 의도된 화면으로 보여야 한다. |

### 2.2 모션 원칙 — **"관문에서는 빠르게, 안에서는 풍성하게"**

전체 모션 수준은 **적극적**으로 정했으나, PRD의 원칙 P1(인트로 3초 통과)·리스크 R2(모션이 이탈 지점이 됨)와 긴장 관계에 있다. 따라서 구간별로 분리 적용한다.

| 구간 | 모션 강도 | 이유 |
|---|---|---|
| **인트로 로고 등장** | 적극적 (v1.2 개정) | 스킵 가능한 연출은 마찰이 아니다. 허브의 성격을 3초 안에 각인시키는 유일한 지면이므로 여기서만 힘을 준다 |
| **인트로 → 리스트 (전환 경로)** | 절제 | 아직 콘텐츠를 못 본 방문자에게, **건너뛸 수 없는** 대기 시간은 순수한 마찰이다 |
| **리스트 내부 (스크롤·호버)** | 적극적 | 이미 진입한 방문자에게는 생동감이 신뢰로 이어진다 |
| **모달 (열기·닫기)** | 중간 | 콘텐츠 가독성이 우선. 등장은 부드럽되 짧게 |

> **판단 기준:** 모션이 "정보에 도달하는 것을 지연시키는가?" → 그렇다면 줄인다.

---

## 3. 컬러 시스템

### 3.1 컬러 토큰

```css
:root {
  /* Background */
  --bg-base:        #121212;   /* 페이지 기본 배경 */
  --bg-surface:     #1A1A1A;   /* 카드, 섹션 블록 */
  --bg-elevated:    #222222;   /* 모달, 떠 있는 요소 */
  --bg-overlay:     rgba(0, 0, 0, 0.72);  /* 모달 뒤 딤 */

  /* Text */
  --text-primary:   #F5F5F5;   /* 제목, 본문 (순백 아님 — 3.3 참조) */
  --text-secondary: #A1A1AA;   /* 요약, 설명 */
  --text-tertiary:  #8A8A8F;   /* 캡션, 메타 (최소 명도 하한) */

  /* Accent */
  --accent:         #3B82F6;   /* 포인트 — 버튼, 링크, 강조 */
  --accent-hover:   #60A5FA;   /* 호버 시 밝게 */
  --accent-subtle:  rgba(59, 130, 246, 0.12);  /* 배경 틴트 */
  --accent-border:  rgba(59, 130, 246, 0.40);  /* 테두리 */

  /* Line */
  --line-subtle:    rgba(255, 255, 255, 0.08);
  --line-default:   rgba(255, 255, 255, 0.14);

  /* On-accent (블루 위에 올라가는 텍스트) */
  --on-accent:      #0A0A0A;   /* 흰색 아님 — 3.3 참조 */
}
```

### 3.2 사용 규칙

| 대상 | 색상 |
|---|---|
| 페이지 배경 | `--bg-base` |
| 카드 표면 | `--bg-surface` |
| 모달 표면 | `--bg-elevated` |
| 프로젝트 제목 | `--text-primary` |
| 한 줄 요약 | `--text-secondary` |
| 카드 번호 `01~08` | `--text-tertiary` (기본) → `--accent` (호버 시) — **§7.6 안 C로 확정** |
| `Coming Soon` 표기 | `--text-tertiary` |
| ENTER 버튼 | 배경 `--accent` + 글자 `--on-accent` |
| 링크 | `--accent` → 호버 시 `--accent-hover` |
| 기술 스택 태그 | 배경 `--accent-subtle` + 글자 `--accent-hover` |

### 3.3 명도 대비 검증 (중요)

| 조합 | 대비율 | 판정 |
|---|---|---|
| `--text-primary` on `--bg-base` | 약 15.8:1 | ✅ AAA |
| `--text-secondary` on `--bg-base` | 약 8.9:1 | ✅ AAA |
| `--text-tertiary` on `--bg-base` | 약 5.4:1 | ✅ AA |
| `--accent` on `--bg-base` | 약 5.1:1 | ✅ AA |
| ⚠️ 흰색 `#FFFFFF` on `--accent` | 약 3.7:1 | ❌ AA 미달 |
| `--on-accent` `#0A0A0A` on `--accent` | 약 5.4:1 | ✅ AA |

**두 가지 결정 근거**

1. **블루 버튼 위에는 흰 글씨를 쓰지 않는다.** 대비 미달이다. 어두운 글자(`--on-accent`)를 쓴다.
2. **텍스트에 순백(`#FFFFFF`)을 쓰지 않는다.** 어두운 배경 위 순백 굵은 글자는 **헤일레이션(halation)** — 글자 가장자리가 번져 보이는 현상 — 을 일으킨다. 로고처럼 크고 굵은 텍스트에서 특히 두드러진다. `#F5F5F5`로 한 단계 낮춘다.

---

## 4. 타이포그래피

### 4.1 서체

| 용도 | 서체 | 대체 |
|---|---|---|
| 영문 UI (`PROJECT HUB`, `ENTER`, `About`, `Contact`) | **Inter** | system-ui, sans-serif |
| 한글 콘텐츠 (프로젝트 제목, 요약, 상세 본문) | **Pretendard** | Apple SD Gothic Neo, sans-serif |

```css
--font-en: 'Inter', system-ui, -apple-system, sans-serif;
--font-kr: 'Pretendard', 'Apple SD Gothic Neo', system-ui, sans-serif;
```

> 두 서체는 골격(x-height, 자폭)이 유사해 한 문장에 섞여도 이질감이 적다. 이것이 혼용을 선택할 수 있는 근거다.

### 4.2 타입 스케일

| 토큰 | 크기 | Weight | 자간 | 용도 |
|---|---|---|---|---|
| `--fs-logo` | `clamp(3rem, 8vw, 6rem)` | 700 | `-0.02em` | 인트로 로고 |
| `--fs-h1` | `clamp(1.75rem, 4vw, 2.5rem)` | 700 | `-0.01em` | 섹션 제목 |
| `--fs-h2` | `clamp(1.25rem, 2.5vw, 1.5rem)` | 600 | `-0.01em` | 모달 제목 |
| `--fs-h3` | `1.125rem` | 600 | `0` | 카드 제목, 모달 소제목 |
| `--fs-body` | `1rem` (16px) | 400 | `0` | 본문 |
| `--fs-small` | `0.875rem` (14px) | 400 | `0` | 요약, 캡션 **(하한선)** |
| `--fs-label` | `0.75rem` (12px) | 500 | `0.08em` | 카드 번호, 태그 — **영문·숫자 전용** |

> **14px 하한 규칙:** 본문 성격의 텍스트는 14px 미만으로 내려가지 않는다. `--fs-label`(12px)은 숫자·영문 라벨에만 허용되는 예외이며, 한글이나 읽어야 할 문장에는 절대 쓰지 않는다. (금지사항 ⑧)

### 4.3 행간

| 용도 | line-height |
|---|---|
| 로고 · 대형 제목 | `1.05` |
| 소제목 | `1.3` |
| 본문 (한글) | `1.7` |
| 본문 (영문) | `1.6` |

> 한글은 영문보다 글자 밀도가 높아 행간을 더 넉넉히 잡아야 읽힌다. 다크 모드에서는 이 차이가 더 크게 체감된다.

### 4.4 로고 타입 상세 스펙

```css
.logo {
  font-family: var(--font-en);
  font-size: clamp(3rem, 8vw, 6rem);
  font-weight: 700;
  letter-spacing: -0.02em;
  color: var(--text-primary);   /* 순백 아님 */
  text-transform: uppercase;
  line-height: 1.05;
}
```

---

## 5. 레이아웃 · 간격

### 5.1 간격 토큰 (4px 기반)

```css
--space-1:  4px;    --space-2:  8px;    --space-3:  12px;
--space-4:  16px;   --space-6:  24px;   --space-8:  32px;
--space-12: 48px;   --space-16: 64px;   --space-24: 96px;
--space-32: 128px;
```

### 5.2 컨테이너

| 항목 | 값 |
|---|---|
| 최대 폭 | `1200px` |
| **캐러셀 랩퍼 최대 폭** | **`1280px`** _(v1.2 개정 — 유일한 예외)_ |
| 좌우 여백 (모바일) | `20px` |
| 좌우 여백 (태블릿 이상) | `40px` |
| 섹션 상하 여백 | `--space-24` (모바일 `--space-16`) |

> **왜 캐러셀만 1280px인가 (v1.2):** v1.1은 슬라이더를 `.container`(1200px) 밖으로 빼 **전체 폭**을 쓰게 했으나, 화면이 넓을수록 카드가 끝없이 이어져 "무한"이 밀도가 아니라 산만함으로 읽혔다. 1280px으로 고정하면 320px 카드 기준 **정확히 3.5장**이 잡혀, 잘린 4번째 카드가 "옆에 더 있다"를 알리는 신호로 안정된다. 양끝 페이드는 그대로 유지한다.

### 5.3 브레이크포인트 & 카드 슬라이더 _(v1.1 개정)_

카드는 컬럼 그리드가 아니라 **화면 폭 전체를 쓰는 가로 무한 슬라이더**로 배치한다.
컬럼 수 대신 **카드 폭**이 반응형 변수다.

| 구간 | 폭 | 카드 폭 | 화면당 노출 |
|---|---|---|---|
| 모바일 | ~767px | `74vw` (최소 260px) | 약 1.3장 |
| 태블릿 | 768~1023px | `320px` | 약 2.5장 |
| 데스크톱 | 1024px~ | `320px` | 4장 이상 |

- 카드 간격(gap): `--space-6` (24px)
- 모바일에서 카드 폭을 `74vw`로 잡는 이유: 다음 카드가 **의도적으로 잘려 보여야** 옆으로 더 있다는 사실이 전달된다. 100vw면 슬라이더인지 알 수 없다.
- ~~슬라이더 컨테이너는 `.container`(1200px)를 벗어나 **전체 폭**을 사용한다.~~ → **v1.2 개정: 캐러셀 랩퍼는 `1280px` 고정** (§5.2 참조). 양끝은 마스크로 페이드시켜 잘림이 아니라 이어짐으로 읽히게 한다.

### 5.4 모서리 반경

```css
--radius-sm: 6px;    /* 태그, 작은 버튼 */
--radius-md: 12px;   /* 카드 */
--radius-lg: 16px;   /* 모달 */
--radius-full: 999px; /* ENTER 버튼(선택) */
```

### 5.5 그림자

다크 모드에서는 그림자가 잘 보이지 않는다. **깊이 표현은 그림자보다 배경 밝기 차이로 처리한다.**

```css
--shadow-card:  0 4px 16px rgba(0, 0, 0, 0.4);
--shadow-modal: 0 24px 64px rgba(0, 0, 0, 0.6);
```

---

## 6. 컴포넌트 스펙

### 6.1 인트로 화면

```
┌──────────────────────────────┐
│                              │
│          vibe coding         │  ← --fs-label, tertiary (v1.2)
│        PROJECT HUB           │  ← --fs-logo, 700, -0.02em
│                              │
│         [ ENTER ]            │  ← accent 배경 + on-accent 글자
│                              │
│          press enter         │  ← 하단 힌트, --fs-label (v1.2)
└──────────────────────────────┘
```

| 항목 | 스펙 |
|---|---|
| 배경 | `--bg-base` |
| 정렬 | 수직·수평 중앙 |
| 서비스명 | **`PROJECT HUB`** (PRD INT-01 유지). `vibe coding`은 로고 위 작은 라벨로만 두며 로고 자체를 대체하지 않는다 _(v1.2, PRD O1 대응)_ |
| eyebrow ↔ 로고 간격 | `--space-4` (16px) |
| 로고 ↔ 버튼 간격 | `--space-12` (48px) |
| ENTER 버튼 크기 | 최소 `160×52px` (터치 영역 44px 이상 확보) |
| ENTER 폰트 | `--font-en`, 14px, 600, 자간 `0.12em`, 대문자 |
| 배경 이미지·태그라인 | **없음** (PRD 확정) |

### 6.2 프로젝트 카드

```
┌────────────────────┐
│                    │
│    Coming Soon     │  ← 썸네일 영역 (16:9)
│                    │
├────────────────────┤
│ 01                 │  ← --fs-label, tertiary
│ 프로젝트 제목       │  ← --fs-h3, primary
│ 한 줄 요약 텍스트   │  ← --fs-small, secondary
└────────────────────┘
```

| 항목 | 스펙 |
|---|---|
| 배경 | `--bg-surface` |
| 반경 | `--radius-md` |
| 내부 여백 | `--space-5` (20px) |
| 썸네일 비율 | `16:9` 고정 |
| 카드 높이 | **모든 카드 동일** (요약 2줄 초과 시 말줄임) |
| 클릭 영역 | 카드 전체 |
| 커서 | `pointer` |

**Coming Soon 표기 (D4 — "비어 있음을 디자인한다")**

- 별도 배지를 만들지 않고 **썸네일 영역 중앙**에 배치한다
- 색상 `--text-tertiary`, `--fs-label`, 자간 `0.12em`, 대문자
- 썸네일 배경은 `--bg-elevated` 단색 또는 미세한 노이즈 텍스처
- 8개가 전부 동일 문구이므로, 시각적으로 **조용하게** 처리한다. 강조하면 미완성 인상만 증폭된다

### 6.3 상세 모달

| 항목 | 스펙 |
|---|---|
| 딤 배경 | `--bg-overlay` |
| 모달 배경 | `--bg-elevated` |
| 최대 폭 | `680px` |
| 최대 높이 | `85vh` (초과 시 내부 스크롤) |
| 반경 | `--radius-lg` |
| 내부 여백 | `--space-8` (모바일 `--space-6`) |
| 모바일 형태 | 바텀시트 (상단만 라운드, 폭 100%) |

**섹션 구조 및 시각 위계** (PRD 확정 순서 준수)

| 순서 | 섹션 | 스타일 |
|---|---|---|
| — | 번호 + 제목 + 한 줄 요약 | 번호 `--fs-label`, 제목 `--fs-h2` |
| 1 | 문제 정의 | 소제목 `--fs-h3` + 본문 `--fs-body` |
| 2 | 타겟 사용자 | 동일 |
| 3 | 핵심 예정 기능 | 불릿 리스트, 불릿 마커는 `--accent` |
| 4 | 사용 기술 스택 | 태그 칩 (`--accent-subtle` 배경) |

- 섹션 간 구분: `--line-subtle` 1px 또는 `--space-8` 여백
- 닫기 버튼: 우측 상단, 최소 `44×44px`

### 6.4 About 섹션 (상단)

| 항목 | 스펙 |
|---|---|
| 위치 | 리스트 화면 최상단, 첫 스크롤 없이 보임 |
| 이름 | `--fs-h1` |
| 역할 | `--fs-body`, `--text-secondary` |
| 소개문 | 2~3문장, `--fs-body`, `--text-secondary`, 최대 폭 `60ch` |
| 톤 | 담백한 서술. 과장 표현 금지 (7장 참조) |

### 6.5 Contact 섹션 (하단)

| 항목 | 스펙 |
|---|---|
| 위치 | 페이지 최하단 (푸터) |
| 이메일 | `--accent` 링크, `mailto:` 연결 |
| 외부 링크 | 텍스트 링크. 새 탭 (`target="_blank" rel="noopener"`) |
| 아이콘 | 사용 시 단색 SVG만. 이모지 금지 |
| 상단 구분선 | `--line-subtle` |

---

## 7. 인터랙션 & 모션

### 7.1 모션 토큰

```css
--ease-out:    cubic-bezier(0.16, 1, 0.3, 1);      /* 등장 */
--ease-inout:  cubic-bezier(0.65, 0, 0.35, 1);     /* 전환 */
--dur-fast:    150ms;   /* 색 변화, 호버 */
--dur-base:    300ms;   /* 등장, 페이드 */
--dur-slow:    600ms;   /* 화면 전환 (상한) */
```

### 7.2 인트로 로고 등장 (SplitText 산포) — v1.2 신설

| 항목 | 스펙 |
|---|---|
| 방식 | 로고를 글자 단위로 쪼개, 각 글자가 흩어진 상태에서 제자리로 수렴 |
| 재생 시간 | **1.2초** |
| 이징 | `power4.out` — 초반에 크게 좁히고 끝에서 정밀하게 안착 |
| 글자별 시작값 | `x` `y` 각 `±200px`, `rotation` `±60°`, `scale` `0.6`, `opacity` `0` |
| 스태거 | `each: 0.03s`, `from: "random"` |
| ENTER 버튼 | 로고 수렴 마지막 0.35초에 겹쳐 `y +12px → 0` 페이드 인 |

**필수 안전장치 (PRD R2 대응)**

- ⚠️ **연출 중 클릭·키 입력 시 즉시 완료 상태로 건너뛴다.** 이 스킵이 있기 때문에 1.2초가 허용된다. 스킵을 빼면 상한은 다시 0.6초다.
- `prefers-reduced-motion: reduce` 설정 시 → 산포를 생략하고 완성 상태로 표시한다
- 화면 하단에 `press enter` 힌트를 두어 즉시 진입 가능함을 알린다

> **왜 `from: "random"` 인가:** 위치만 난수화하고 시작 순서를 좌→우로 두면 파도처럼 읽혀 "정돈된 연출"이 된다. 흩어짐을 의도했다면 순서까지 섞어야 한다.

### 7.3 인트로 → 리스트 전환 (줌 인)

| 항목 | 스펙 |
|---|---|
| 방식 | 로고가 확대되며 화면을 통과, 동시에 페이드 아웃 |
| 재생 시간 | **0.6초 상한** (초과 금지) |
| 이징 | `--ease-inout` — 초반에 빠르게 빠져나감 |
| 로고 스케일 | `1 → 1.6` |
| 인트로 opacity | `1 → 0` |
| 리스트 등장 | 마지막 0.3초에 겹쳐서 페이드 인 |

**필수 안전장치 (PRD R2 대응)**

- ⚠️ **연출 중 클릭·키 입력 시 즉시 건너뛰고 리스트로 진입한다.** 줌 연출은 화면 전체가 움직이기 때문에, 실제 0.6초여도 체감은 더 길다. 스킵이 없으면 마찰이 된다.
- `prefers-reduced-motion: reduce` 설정 시 → 줌을 생략하고 즉시 전환한다

### 7.4 리스트 내부 모션 (적극적 적용 구간)

| 대상 | 효과 | 스펙 |
|---|---|---|
| 카드 최초 등장 | 스태거 페이드 업 | `translateY(16px) → 0`, 카드당 `60ms` 지연, `--dur-base` |
| **카드 무한 루프** | 등속 좌측 이동 | **`52px/sec`, `ease: none`, `repeat: -1`** |
| **루프 정지·재개** | `timeScale` 트윈 | **`1 ↔ 0`, `450ms`, `power2.out`** |
| **드래그 관성** | 던진 뒤 감속 | **`900ms`, `power3.out`** |
| 섹션 스크롤 진입 | 페이드 업 | IntersectionObserver, 1회만 실행 |
| ENTER 버튼 호버 | 배경 밝기 상승 | `--accent` → `--accent-hover`, `--dur-fast` |
| 링크 호버 | 색 전환 + 밑줄 | `--dur-fast` |

**슬라이더 모션 규칙 (v1.1 신설)**

- **정지는 감속으로 처리한다.** `pause()`로 즉시 세우면 관성이 끊겨 고장난 것처럼 보인다. `timeScale`을 0까지 트윈해 미끄러지듯 멈춘다.
- **속도는 읽는 속도를 앞지르지 않는다.** 52px/sec는 카드 한 장(320px)이 지나가는 데 약 6초다. 제목과 요약을 읽기에 충분하다.
- `ease: none` 필수 — 가감속이 들어가면 루프 이음매에서 속도가 튄다.
- `prefers-reduced-motion: reduce` → 자동 이동을 하지 않고 **가로 스크롤 목록**으로 대체한다. (A5)

### 7.5 모달 모션

**v1.2 개정 — 모달은 "떠오르는 창"이 아니라 "커지는 카드"다.**

| 동작 | 스펙 |
|---|---|
| 열기 | 클릭한 카드의 화면 좌표에서 출발해 중앙 목표 크기까지 `left·top·width·height` 확대. `0.55초`, `power2.inOut` |
| 본문 등장 | 확대 시작 후 `0.4초` 지점부터 `opacity 0→1`, `0.3초`, stagger `0.05s` |
| 닫기 | 역방향 `0.45초`. 본문은 먼저 `0.15초`에 사라진다 (닫기는 항상 빠르게) |
| 딤 배경 | `--dur-base` 페이드, `--bg-overlay` |
| ~~열기 (데스크톱)~~ | ~~`opacity 0→1`, `scale 0.96→1`~~ → 위로 대체 |

**두 가지 구현 규칙**

- **`scale`이 아니라 `width`/`height`를 애니메이션한다.** `scale`로 키우면 카드 안의 글자까지 함께 늘어나 흐려진다. 상세 모달은 읽히는 것이 목적이므로 텍스트가 목표 크기에서 선명해야 한다.
- **원본 카드를 옮기지 않고, 화면에 고정 패널을 겹쳐 키운다.** 카드는 이동 중인 트랙 안에 있어 좌표가 계속 변한다. 카드 자체를 꺼내면 트랙의 `transform`과 얽혀 위치 계산이 어긋난다. 원본은 그 자리에서 잠시 비우고(`opacity: 0`), 닫을 때 되돌린다.

> **왜 §7.6에서 A(떠오름)를 뺐는데 여기서는 "앞으로 나오게" 하는가:** 호버는 *지나가는 반응*이라 이동 중인 카드와 겹치면 흔들림이 된다. 클릭은 *멈춘 뒤에 일어나는 전환*이다. 루프가 정지한 상태에서는 축이 하나뿐이므로 겹침 문제가 생기지 않는다.

### 7.6 카드 호버 효과 — **확정: B + C** _(v1.1)_

> **결정: B(테두리 발광) + C(번호 강조).** A(떠오름)는 제외한다.
> 카드가 이미 가로로 이동 중이므로 세로 리프트가 겹치면 움직임이 두 축으로 분해되어
> 반응이 아니라 **흔들림**으로 읽힌다. 또한 슬라이더에서는 **호버 시 이동이 멈추는 것 자체가
> 가장 강한 피드백**이라, §7.5의 원래 우려("정적인 시안처럼 보일 위험")는 해소된 상태다.
>
> 되돌리려면 `.card__btn:hover`에 `transform: translateY(-6px)` + `--shadow-card` 한 줄을 추가한다.

아래는 원본 후보표다.

| 후보 | 스펙 | 특징 |
|---|---|---|
| **A. 떠오름** | `transform: translateY(-6px)` + `--shadow-card`, `--dur-fast` | 가장 무난. 물리적 반응감 |
| **B. 테두리 발광** | `border: 1px solid --accent-border` → `--accent` | 다크 모던과 궁합 최상 |
| **C. 번호 강조** | `01~08` 색상 `--text-tertiary` → `--accent` | 절제된 방식. 눈에 덜 띔 |
| **D. 복합 (A+B+C)** | 위 3개 동시 적용 | 적극적 모션 기조와 일치 |

> **선택 시 판단 기준:** 8개 카드가 전부 Coming Soon이므로, 호버 반응이 "이 사이트는 살아있다"는 거의 유일한 신호다. 지나치게 절제하면 정적인 시안처럼 보일 위험이 있다.

**공통 규칙 (어느 안을 고르든 적용)**
- 전환 시간은 `--dur-fast` (150ms) — 호버는 즉각적이어야 한다
- 터치 디바이스에서는 호버 대신 `:active` 상태로 대체한다
- `transform`과 `opacity`만 애니메이션한다 (레이아웃 재계산 방지)

---

## 8. 금지사항 (Don'ts)

> 구현 중 판단이 흔들릴 때 돌아오는 기준이다.

### 8.1 확정 금지 항목

| # | 금지 | 이유 | 대안 |
|---|---|---|---|
| **① 이모지 사용 금지** | 텍스트·UI·아이콘 어디에도 이모지를 쓰지 않는다 | 다크 모던의 절제된 톤을 즉시 무너뜨린다. 플랫폼별 렌더링이 달라 통제도 불가능하다 | 단색 SVG 아이콘 또는 텍스트 라벨 |
| **② 이탤릭 금지** | `font-style: italic` 사용 금지 | Inter/Pretendard 모두 이탤릭에서 골격이 흐트러진다. 다크 배경에서는 가독성이 더 떨어진다 | 강조는 Weight(600) 또는 `--accent` 색상으로 |
| **③ 본문 14px 미만 금지** | 읽어야 할 텍스트는 14px 아래로 내리지 않는다 | 어두운 배경에서 작은 글자는 밝은 배경보다 훨씬 읽기 어렵다 | 12px는 `--fs-label`(영문·숫자 라벨)에만 예외 허용 |
| **④ 과장된 자기소개 금지** | "혁신적인", "열정적인", "최고의", "글로벌", "차별화된" 등 | 8개가 전부 Coming Soon인 상태에서 과장은 신뢰를 깎는다. 결과물이 없을수록 문장은 담백해야 설득력이 생긴다 | 사실 기반 서술 — 무엇을 하고 있는지, 무엇에 관심 있는지 |

### 8.2 About 문장 — 나쁜 예 / 좋은 예

| 구분 | 예시 |
|---|---|
| ❌ 나쁜 예 | "혁신적인 사고로 사용자 경험을 재정의하는 열정적인 디자이너입니다!" |
| ✅ 좋은 예 | "사용자 리서치를 기반으로 제품을 기획합니다. 현재 8개의 프로젝트를 준비하고 있으며, 각 프로젝트의 문제 정의와 접근 방식을 이곳에 기록합니다." |

> 좋은 예는 ④(과장 금지)와 ①(이모지 금지)을 모두 지키면서, PRD 리스크 R1(미완성 인상)에 대한 대응까지 겸한다.

### 8.3 시스템상 자연히 따르는 제약 (참고)

명시적으로 선택하진 않았으나, 위 결정들로부터 자동으로 도출되는 제약이다.

- 포인트 컬러(`#3B82F6`) 외의 유채색 추가 → 원칙 D3에 위배
- Inter / Pretendard 외 서체 추가 → 4.1 정의에 위배
- 흰색(`#FFFFFF`) 텍스트를 블루 배경 위에 사용 → 3.3 대비 미달

---

## 9. 접근성 체크리스트

| # | 항목 | 기준 |
|---|---|---|
| A1 | 텍스트 명도 대비 | 본문 4.5:1 이상, 대형 텍스트 3:1 이상 (3.3 검증표 준수) |
| A2 | 키보드 탐색 | Tab 이동, Enter 진입/실행, ESC 모달 닫기 |
| A3 | 포커스 표시 | `outline: 2px solid var(--accent)` + `offset: 2px` — **제거 금지** |
| A4 | 터치 영역 | 최소 `44×44px` |
| A5 | 모션 감소 대응 | `prefers-reduced-motion: reduce` → 줌·스태거 등 전 애니메이션 비활성 |
| A6 | 이미지 대체 텍스트 | 모든 썸네일에 `alt` 제공 (장식용은 `alt=""`) |
| A7 | 모달 포커스 트랩 | 열림 시 모달 내부로 포커스 이동, 닫힘 시 원래 카드로 복귀 |

---

## 10. 구현 참고 — CSS 변수 전체

```css
:root {
  /* Color */
  --bg-base: #121212;
  --bg-surface: #1A1A1A;
  --bg-elevated: #222222;
  --bg-overlay: rgba(0,0,0,0.72);
  --text-primary: #F5F5F5;
  --text-secondary: #A1A1AA;
  --text-tertiary: #8A8A8F;
  --accent: #3B82F6;
  --accent-hover: #60A5FA;
  --accent-subtle: rgba(59,130,246,0.12);
  --accent-border: rgba(59,130,246,0.40);
  --on-accent: #0A0A0A;
  --line-subtle: rgba(255,255,255,0.08);
  --line-default: rgba(255,255,255,0.14);

  /* Font */
  --font-en: 'Inter', system-ui, sans-serif;
  --font-kr: 'Pretendard', 'Apple SD Gothic Neo', system-ui, sans-serif;
  --fs-logo: clamp(3rem, 8vw, 6rem);
  --fs-h1: clamp(1.75rem, 4vw, 2.5rem);
  --fs-h2: clamp(1.25rem, 2.5vw, 1.5rem);
  --fs-h3: 1.125rem;
  --fs-body: 1rem;
  --fs-small: 0.875rem;
  --fs-label: 0.75rem;

  /* Space */
  --space-1: 4px;   --space-2: 8px;   --space-3: 12px;
  --space-4: 16px;  --space-5: 20px;  --space-6: 24px;
  --space-8: 32px;  --space-12: 48px; --space-16: 64px;
  --space-24: 96px; --space-32: 128px;

  /* Radius */
  --radius-sm: 6px;
  --radius-md: 12px;
  --radius-lg: 16px;
  --radius-full: 999px;

  /* Shadow */
  --shadow-card: 0 4px 16px rgba(0,0,0,0.4);
  --shadow-modal: 0 24px 64px rgba(0,0,0,0.6);

  /* Motion */
  --ease-out: cubic-bezier(0.16, 1, 0.3, 1);
  --ease-inout: cubic-bezier(0.65, 0, 0.35, 1);
  --dur-fast: 150ms;
  --dur-base: 300ms;
  --dur-slow: 600ms;
}

@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    transition-duration: 0.01ms !important;
  }
}
```

---

## 부록 A. 미확정 이슈 (Open Issues)

| # | 이슈 | 상태 | 참조 |
|---|---|---|---|
| D-O1 | 카드 호버 효과 (A/B/C/D 중 택 1) | ✅ **확정 — B + C** (A 제외, 근거는 7.5) | 7.5 |
| D-O2 | 썸네일 처리 방식 — 단색 / 노이즈 텍스처 / 타이포 조합 | ✅ **확정 — `--bg-elevated` 단색 + 45° 미세 텍스처**(alpha 0.015). 외부 이미지 없이 CSS로만 처리 | 6.2 |
| D-O3 | ENTER 버튼 형태 — 사각 vs 알약 | ✅ **확정 — 알약(`--radius-full`)** | 6.1 |
| D-O4 | 데스크톱 카드 그리드 4열 vs 3열 | ⛔ **소멸** — v1.1에서 슬라이더로 전환되어 컬럼 개념이 사라짐 | 5.3 |
| D-O5 | Contact 아이콘 사용 여부 | ✅ **확정 — 아이콘 미사용, 텍스트 링크만.** 이모지 금지(①)와 통제 가능성을 함께 만족 | 6.5 |
| D-O6 | 서비스명 표기 `PROJECT HUB` vs `PROJUCT HUB` | ⚠️ **미확정** — 현재 `PROJECT HUB`로 구현됨 | PRD O1 |

## 부록 B. 결정 근거 추적 (Evidence Chain)

| 결정 | 근거 |
|---|---|
| 다크 모던 채택 | 사용자 선택. 부수 효과로 Coming Soon 썸네일의 빈약함을 완화 (PRD R1) |
| 차콜(`#121212`) 선택, 순수 블랙 미채택 | 순수 블랙은 대비가 과해 장시간 열람 시 피로. 차콜이 다크 모던의 표준값 |
| 로고에 순백 대신 `#F5F5F5` | 어두운 배경 + 굵은 대문자 조합의 헤일레이션 방지 |
| 블루 버튼 위 어두운 글자 | 흰색은 대비 3.7:1로 AA 미달, 어두운 글자는 5.4:1로 통과 |
| 모션을 구간별로 분리 | "적극적" 선택과 PRD P1·R2의 긴장 해소. 진입 마찰과 내부 생동감을 분리 관리 |
| 줌 전환에 스킵 기능 필수화 | 줌은 화면 전체가 움직여 체감 시간이 실제보다 길다 |
| 12px을 영문 라벨에만 허용 | 14px 하한(금지사항 ⑧)과 카드 번호 디자인 요구 사이의 절충 |
| 이모지 금지를 아이콘까지 확대 | 플랫폼별 렌더링 차이로 통제 불가. 절제된 톤과 충돌 |
| 과장 표현 금지를 About의 핵심 규칙으로 | 결과물이 없을수록 담백한 서술이 신뢰를 만듦 (PRD R1 대응 2와 연결) |
| 그리드 → 무한 슬라이더 (v1.1) | 8개 전부 Coming Soon인 정지 그리드는 "정적인 시안"으로 읽힘. §2.2가 리스트 내부에 부여한 "적극적" 모션 기조와도 일치. 대가는 PRD R4 |
| 호버 리프트(A) 제외 | 카드가 이미 x축으로 이동 중. y축 리프트가 겹치면 반응이 아니라 흔들림으로 읽힘 |
| 복제 카드에 `aria-hidden` + `tabindex="-1"` | 무한 루프는 DOM 복제로 구현됨. 방치하면 스크린리더가 같은 카드를 3번 읽고 Tab이 24번 멈춤 |
| 모바일 카드 폭 74vw | 다음 카드가 잘려 보여야 슬라이더임이 전달됨. 100vw면 단일 카드로 오인 |
| 로고 그라디언트를 흰색→블루 램프로 한정 | 흐르는 그라디언트라는 기법은 D3가 금지하지 않음. 금지 대상은 색이므로 램프를 accent 계열로 제한 |

---

*문서 끝*
